#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: print the alphabet letters
 *
 * Return: Always 0 (Success)
*/

int main(void)
{
	for (i = 'a' ; i <= 'z' ; i++)
		putchar(i);
	putchar("\n");
	return (0);
}
